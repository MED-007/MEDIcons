[build]
  command = "npm run build"
  publish = ".next"
  functions = "netlify/functions"

[dev]
  command = "npm run dev"
  port = 3000
  targetPort = 3000

[functions]
  node_bundler = "esbuild"

[plugins]
  package = "@netlify/plugin-nextjs"
